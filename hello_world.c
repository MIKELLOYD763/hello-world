#include <stdio.h>  // This is the header file for input and output functions like printf

int main() {
    // The main function is the entry point of every C program
    printf("Hello, World!\n");  // This prints "Hello, World!" followed by a newline
    return 0;  // The program returns 0 to indicate successful execution
}
